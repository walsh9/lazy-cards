{{#if choosingGraphics}}
  {{emoji-selector-graphics
    emojiList=emojiList
    graphicSelectAction=(action "setGraphic")
    cancelAction=(action "cancel")}}
{{else}}
  {{emoji-selector-categories
    categoryList=categoryList
    categorySelectAction=(action "setCategory")
    cancelAction=(action menuCancelAction)}}
{{/if}}