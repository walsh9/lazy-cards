{{#if choosingGraphics}}
  {{emoji-selector-graphics
    emojiList=emojiList
    selectEmoji=(action 'setGraphic')
    cancelHandler=(action 'cancel')}}
{{else}}
  {{emoji-selector-categories
    categoryList=categoryList
    selectCategory=(action 'setCategory')
    cancelHandler=cancelHandler}}
{{/if}}
